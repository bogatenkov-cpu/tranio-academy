# Как обновить деплой на Vercel

## 🚀 Автоматическое обновление (Рекомендуется)

Vercel **автоматически** обновляет деплой при каждом push в GitHub!

### Шаги:

1. **Убедитесь, что все изменения закоммичены и отправлены в GitHub:**
   ```bash
   cd /Users/evgeniibogatenkov/Desktop/tranio-academy
   
   # Проверьте статус
   git status
   
   # Если есть изменения, добавьте их
   git add .
   git commit -m "Update trainer and exam pages"
   
   # Отправьте на GitHub
   git push origin main
   ```

2. **Vercel автоматически начнёт деплой:**
   - Зайдите на https://vercel.com
   - Откройте ваш проект `tranio-academy`
   - Вы увидите новый деплой в разделе "Deployments"
   - Статус будет: "Building..." → "Ready" (обычно 2-3 минуты)

3. **Проверьте результат:**
   - После завершения деплоя сайт автоматически обновится
   - Новый деплой станет активным (production)

---

## 🔄 Ручное обновление (если нужно)

Если автоматический деплой не сработал:

### Вариант 1: Через веб-интерфейс Vercel

1. Зайдите на https://vercel.com
2. Откройте проект `tranio-academy`
3. Нажмите на вкладку **"Deployments"**
4. Найдите последний деплой и нажмите **"Redeploy"** (три точки → Redeploy)

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите (если еще не вошли)
vercel login

# Перейдите в директорию проекта
cd /Users/evgeniibogatenkov/Desktop/tranio-academy

# Запустите деплой
vercel --prod
```

---

## ✅ Проверка, что всё работает

### 1. Проверьте, что код на GitHub актуальный:

```bash
cd /Users/evgeniibogatenkov/Desktop/tranio-academy
git log --oneline -5  # Последние 5 коммитов
```

### 2. Проверьте статус деплоя на Vercel:

1. Зайдите на https://vercel.com
2. Откройте проект
3. Вкладка **"Deployments"** — должны быть все ваши коммиты
4. Если последний коммит не задеплоен — нажмите "Redeploy"

### 3. Проверьте логи деплоя:

- В Vercel → Deployments → выберите деплой → "View Function Logs"
- Если есть ошибки — они будут видны там

---

## 🔍 Если деплой не обновляется автоматически

### Проблема: Vercel не подключен к GitHub репозиторию

**Решение:**
1. Vercel → Settings → Git
2. Проверьте, что репозиторий подключен
3. Если нет — нажмите "Connect Git Repository"

### Проблема: Ветка не отслеживается

**Решение:**
1. Vercel → Settings → Git
2. Убедитесь, что отслеживается ветка `main` (или `master`)

### Проблема: Ошибки при сборке

**Решение:**
1. Проверьте логи деплоя в Vercel
2. Убедитесь, что локально `npm run build` работает без ошибок:
   ```bash
   npm run build
   ```

---

## 📝 Быстрая команда для обновления:

```bash
# Все в одной команде
cd /Users/evgeniibogatenkov/Desktop/tranio-academy && \
git add . && \
git commit -m "Update project" && \
git push origin main && \
echo "✅ Код отправлен на GitHub. Vercel автоматически обновит деплой через 2-3 минуты!"
```

---

## 🎯 Итоговая схема работы:

```
Локальные изменения
    ↓
git add . && git commit -m "..."
    ↓
git push origin main
    ↓
GitHub получает обновления
    ↓
Vercel автоматически видит изменения
    ↓
Vercel запускает сборку (npm run build)
    ↓
Деплой готов через 2-3 минуты
    ↓
Сайт обновлён! 🎉
```

---

## 💡 Полезные ссылки:

- Vercel Dashboard: https://vercel.com/dashboard
- Документация Vercel: https://vercel.com/docs
- GitHub репозиторий: https://github.com/YOUR_USERNAME/tranio-academy


