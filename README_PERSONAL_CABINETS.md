# 🎓 Личные кабинеты Tranio Academy

## Что добавлено:

### 🔐 Система аутентификации
- **Регистрация** с email + пароль
- **Вход** в существующий аккаунт
- **Восстановление пароля** через email
- **Автоматический выход** при истечении сессии

### 💾 База данных Supabase
- **PostgreSQL** база данных в облаке
- **Row Level Security** — каждый видит только свои данные
- **Автоматические триггеры** для обновления данных
- **Real-time** возможности (можно добавить позже)

### 📊 Хранение данных
Все данные пользователя теперь хранятся в облаке:
- Баллы и прогресс
- Стрик правильных ответов
- Результаты экзаменов
- История активности
- Пройденные уроки
- Изученные карточки

### 🔄 Миграция данных
При первом входе старые данные из `localStorage` **автоматически** мигрируют в облако!

### 👤 Личный кабинет
Обновленный профиль с:
- Статистикой из базы данных
- Рейтингом пользователей
- Историей активности
- Прогрессом по странам
- Кнопкой выхода

## Файловая структура:

```
tranio-academy/
├── lib/
│   ├── supabase/
│   │   ├── client.ts           # Клиент Supabase
│   │   ├── database.types.ts   # TypeScript типы БД
│   │   └── schema.sql          # SQL схема базы данных
│   ├── hooks/
│   │   └── useProgress.ts      # Хук для работы с прогрессом
│   └── utils/
│       └── migrateLocalStorage.ts  # Утилита миграции
├── contexts/
│   └── AuthContext.tsx         # Контекст аутентификации
├── components/
│   └── MigrationHandler.tsx    # Компонент миграции данных
├── app/
│   ├── layout.tsx              # Обновлен с AuthProvider
│   ├── page.tsx                # Новая страница входа/регистрации
│   ├── profile/page.tsx        # Обновленный профиль
│   └── auth/
│       └── callback/route.ts   # Обработчик OAuth callback
└── .env.local                  # Ключи Supabase (создать!)
```

## Как использовать в коде:

### Получить текущего пользователя:

```typescript
import { useAuth } from '@/contexts/AuthContext';

function MyComponent() {
  const { user, loading } = useAuth();
  
  if (loading) return <div>Загрузка...</div>;
  if (!user) return <div>Войдите в аккаунт</div>;
  
  return <div>Привет, {user.email}!</div>;
}
```

### Работать с прогрессом:

```typescript
import { useProgress } from '@/lib/hooks/useProgress';

function LessonPage() {
  const { progress, addPoints, addActivity } = useProgress('thailand');
  
  const handleCorrectAnswer = async () => {
    await addPoints(10);
    await addActivity('trainer', 'Правильный ответ', 10);
  };
  
  return <div>Ваши баллы: {progress.points}</div>;
}
```

### Защитить страницу:

```typescript
'use client';
import { useAuth } from '@/contexts/AuthContext';
import { useRouter } from 'next/navigation';
import { useEffect } from 'react';

function ProtectedPage() {
  const { user, loading } = useAuth();
  const router = useRouter();
  
  useEffect(() => {
    if (!loading && !user) {
      router.push('/');
    }
  }, [user, loading, router]);
  
  if (loading) return <div>Загрузка...</div>;
  if (!user) return null;
  
  return <div>Защищенный контент</div>;
}
```

## API хука useProgress:

```typescript
const {
  progress,              // Объект с прогрессом
  loading,               // Загрузка
  updateProgress,        // Обновить прогресс
  addPoints,             // Добавить баллы
  updateStreak,          // Обновить стрик
  addCompletedLesson,    // Отметить урок как пройденный
  addStudiedCard,        // Добавить изученную карточку
  updateExamStats,       // Обновить статистику экзаменов
  addActivity,           // Добавить активность
  reload,                // Перезагрузить прогресс
} = useProgress('thailand');
```

## Что дальше?

### Готово к использованию:
- ✅ Регистрация и вход
- ✅ Профиль пользователя
- ✅ Хранение прогресса
- ✅ Миграция данных

### Нужно интегрировать в остальные страницы:
- 🔲 Страница тренажера (`/countries/thailand/trainer`)
- 🔲 Страница экзаменов (`/countries/thailand/exam`)
- 🔲 Страницы уроков (`/countries/thailand/theory/...`)

### Для интеграции на странице:

1. Добавьте проверку авторизации:
```typescript
const { user, loading } = useAuth();
```

2. Замените `localStorage` на `useProgress`:
```typescript
const { progress, addPoints } = useProgress('thailand');
```

3. Используйте методы хука вместо прямой работы с localStorage

## Преимущества новой системы:

✅ **Данные в облаке** — не теряются при очистке браузера  
✅ **Мультиустройство** — учитесь с телефона, планшета, компьютера  
✅ **Безопасность** — данные защищены Row Level Security  
✅ **Масштабируемость** — готово к росту числа пользователей  
✅ **Аналитика** — легко добавить статистику и отчеты  
✅ **Резервное копирование** — Supabase делает бэкапы автоматически  

## Стоимость:

**Бесплатный тариф Supabase включает:**
- 500 МБ хранилища базы данных
- 1 ГБ трафика
- 50,000 активных пользователей в месяц
- 2 ГБ хранилища файлов

**Этого хватит для тестирования и первых тысяч пользователей!**

---

**Следующий шаг:** Прочитайте `ИНСТРУКЦИЯ_НАСТРОЙКА_SUPABASE.md` для настройки проекта!
