# Как опубликовать Tranio Academy на домене

## Вариант 1: Vercel (Рекомендуется) ⭐

### Шаги:

1. **Подготовьте код:**
   ```bash
   # Убедитесь, что все изменения закоммичены
   git add .
   git commit -m "Prepare for deployment"
   git push origin main
   ```

2. **Создайте аккаунт на Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub (рекомендуется)

3. **Подключите репозиторий:**
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий `tranio-academy`
   - Vercel автоматически определит Next.js

4. **Настройки проекта:**
   - **Framework Preset:** Next.js (автоматически)
   - **Root Directory:** `./` (по умолчанию)
   - **Build Command:** `npm run build` (по умолчанию)
   - **Output Directory:** `.next` (по умолчанию)
   - **Install Command:** `npm install` (по умолчанию)

5. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения сборки (2-3 минуты)
   - Получите бесплатный домен: `tranio-academy.vercel.app`

6. **Подключение своего домена:**
   - В настройках проекта → Domains
   - Добавьте ваш домен (например, `academy.tranio.ru`)
   - Следуйте инструкциям по настройке DNS

---

## Вариант 2: Netlify

### Шаги:

1. **Создайте файл `netlify.toml` в корне проекта:**
   ```toml
   [build]
     command = "npm run build"
     publish = ".next"
   
   [[plugins]]
     package = "@netlify/plugin-nextjs"
   ```

2. **Подготовьте код:**
   ```bash
   git add .
   git commit -m "Add Netlify config"
   git push origin main
   ```

3. **Создайте аккаунт на Netlify:**
   - Перейдите на https://netlify.com
   - Войдите через GitHub

4. **Подключите репозиторий:**
   - "Add new site" → "Import an existing project"
   - Выберите репозиторий
   - Настройки:
     - Build command: `npm run build`
     - Publish directory: `.next`

5. **Деплой:**
   - Нажмите "Deploy site"
   - Получите домен: `tranio-academy.netlify.app`

---

## Вариант 3: Railway

### Шаги:

1. **Создайте аккаунт на Railway:**
   - https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - "New Project" → "Deploy from GitHub repo"
   - Выберите репозиторий

3. **Настройки:**
   - Railway автоматически определит Next.js
   - Добавьте переменные окружения (если нужны)

4. **Деплой:**
   - Railway автоматически задеплоит проект
   - Получите домен: `tranio-academy.up.railway.app`

---

## Важные замечания:

### ⚠️ Перед деплоем проверьте:

1. **Сборка работает локально:**
   ```bash
   npm run build
   ```

2. **Нет ошибок:**
   ```bash
   npm run lint
   ```

3. **Все зависимости установлены:**
   - Убедитесь, что `package.json` содержит все нужные пакеты

### 🔒 Безопасность:

- Не коммитьте `.env` файлы с секретами
- Используйте переменные окружения в настройках деплоя

### 📝 После деплоя:

1. Проверьте работу сайта на предоставленном домене
2. Настройте кастомный домен (если нужно)
3. Настройте SSL сертификат (обычно автоматически)

---

## Быстрый старт с Vercel (CLI):

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Деплой
vercel

# Продакшн деплой
vercel --prod
```

---

## Поддержка:

- Vercel Docs: https://vercel.com/docs
- Netlify Docs: https://docs.netlify.com
- Railway Docs: https://docs.railway.app


