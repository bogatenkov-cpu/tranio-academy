# Обновление Next.js для исправления уязвимости CVE-2025-66478

## ✅ Что было сделано:

Я обновил версию Next.js в `package.json`:
- **Было:** `15.5.4` (уязвимая версия)
- **Стало:** `16.1.1` (последняя версия, рекомендованная Vercel)

Также обновлён `eslint-config-next` до `16.1.1`.

---

## 🚀 Что нужно сделать сейчас:

### 1. Обновите зависимости:

```bash
cd /Users/evgeniibogatenkov/Desktop/tranio-academy

# Обновите Next.js и зависимости
npm install
```

### 2. Проверьте, что всё работает локально:

```bash
# Проверьте сборку
npm run build

# Запустите локально (опционально)
npm run dev
```

### 3. Закоммитьте и отправьте изменения:

```bash
# Добавьте изменения
git add package.json package-lock.json

# Создайте коммит
git commit -m "Security: Update Next.js to 16.1.1 (fix CVE-2025-66478)"

# Отправьте на GitHub
git push origin main
```

### 4. Vercel автоматически обновит деплой:

- После `git push` Vercel увидит изменения
- Запустит новую сборку с обновлённой версией
- Уязвимость будет исправлена

---

## 🔍 Проверка после обновления:

1. **Проверьте версию локально:**
   ```bash
   npm list next
   ```
   Должно показать: `next@16.1.1`

2. **Проверьте деплой на Vercel:**
   - Зайдите на https://vercel.com
   - Откройте проект → Deployments
   - Убедитесь, что последний деплой успешен
   - Ошибка об уязвимости должна исчезнуть

---

## 📝 Что это за уязвимость?

**CVE-2025-66478** — критическая уязвимость в Next.js, которая может позволить удалённое выполнение кода в приложениях, использующих React Server Components с App Router.

**Исправлено в версиях:**
- 15.5.7 (для 15.x)
- 16.0.7+ (для 16.x)
- 16.1.1 (последняя версия, рекомендованная Vercel)

---

## ⚠️ Важно:

Если ваше приложение было доступно в сети до обновления, рекомендуется:
1. Сменить все секреты (API ключи, токены и т.д.)
2. Проверить логи на подозрительную активность

---

## ✅ После обновления:

- ✅ Уязвимость исправлена
- ✅ Приложение безопасно
- ✅ Vercel больше не будет показывать предупреждение

